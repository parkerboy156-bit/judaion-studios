# CLAUDE.md - JUDAION Studios Archive[cite: 1]

<!-- Keep this file under 170 lines. Focus ONLY on context Claude cannot infer from code. -->

## 🎯 Project Overview
- **Stack:** Next.js 16 (App Router), React 19, TypeScript, Tailwind CSS v4, Supabase, Oracle Cloud Storage[cite: 1].
- **Architecture:** Page files (`app/<route>/page.tsx`) are thin server components that only import and render a corresponding `"use client"` shell from `components/<Page>Client.tsx` where all interactive logic resides[cite: 1].
- **Routes:** `/` (Home), `/services`, `/thenarrative`, `/methodology`, `/contact`, `/projectarchive`, `/archivecatalogue`, `/tier-1`, `/tier-2`, `/tier-3`, `/login`, `/adminportal`.
- **Entry Sequence:** `layout.tsx` → `<Providers>` → `<ClientShell>` → `<Layout>`[cite: 1]. Content stays hidden until `<IntroLoader>` completes its global fade-in animation[cite: 1]. `<RotateNotice>` renders globally and is visibility-controlled by a CSS landscape media query. `<InspectionLoader>` is a page-level full-screen loader (designed loading-screen video + corner J-logo, via the shared `<LoaderScreen>`) used by Tier and Archive pages to gate their assemble animations.

---

## 🛠️ Essential Commands
> **Rule:** Run from the root directory. There is no test suite configured; use build/lint for verification[cite: 1].

| Action | Command | Scope / Execution Directory |
| :--- | :--- | :--- |
| **Development** | `npm run dev` | Root (Localhost:3000)[cite: 1] |
| **Verify Build** | `npm run build` | Root (Forces typecheck and compilation)[cite: 1] |
| **Lint & Fix** | `npm run lint` | Root (ESLint check)[cite: 1] |
| **Optimize image(s)** | `npm run optimize -- <file\|folder> [maxWidth] [quality] [--to fmt]` | Root — `scripts/optimize.mjs`; in-place re-encode (downscale + tight compression, alpha preserved), only overwrites if smaller. `--to avif\|webp\|png\|jpg` converts format & deletes the original. Defaults: AVIF q50 / WebP q82 / JPEG q80. |

---

## 🧠 Collaborative Engagement & AI Behavior Rules
To maximize code quality and prevent technical debt, you must follow these cognitive rules:

1. **Be an Engineering Peer, Not a Yes-Man:** Do not blindly execute bad, redundant, or unoptimized requests. If a request compromises performance, token-efficiency, scale, or the clean dark-UI aesthetic, **challenge it directly**. Propose a superior alternative.
2. **Feature Interviewing — Required Before Any New Feature:** When the user asks to add a feature, **do not write code immediately**. First, ask short targeted questions to confirm: what it should do, where it should live, and how it should behave on mobile. Only proceed once there is clear alignment.
3. **Structural Soundness Check:** Before implementing any request, evaluate whether it is structurally sound and practical given the existing architecture. If a request would create technical debt, break existing patterns, conflict with the component hierarchy, or cause layout regressions — say so directly and propose a cleaner alternative. Do not silently implement a bad approach just because it was asked for.
4. **Mobile-First & Resolution Awareness:** Every UI change must account for the `<1024px` (mobile) breakpoint. Before marking any task complete, mentally verify that the change looks correct and is usable at mobile widths. If a change only makes sense at desktop, flag the mobile behaviour explicitly. Additionally, actively scan touched code for CSS that is brittle at non-standard resolutions — hardcoded `px` heights on fluid containers, fixed widths that will overflow, viewport-unit assumptions that break on ultrawide or small laptop screens, and magic numbers with no responsive fallback. Flag any found, explain why they will break, and suggest the fix (e.g. `min-h` instead of `h`, `clamp()`, `max-w` guards, or responsive Tailwind variants).
5. **Proactive Optimization Checks:** Before delivering code, review it for asset optimization (e.g., proper Next.js image loading, bypassing Vercel body limits via presigned URLs) and token usage[cite: 1].
6. **Architectural Cohesion:** Ensure components remain modular and semantic. Maintain strict data-flow separation: logic stays in the client shells, while routing/metadata stays in the server pages[cite: 1].
7. **Terse Comments Only:** When adding a new feature or fix, comment it with ONE short descriptive sentence max — never a multi-line block explaining the reasoning, alternatives considered, or history. If a comment needs more than one line, the code needs a clearer name instead.

---

## ⚡ Token-Saving & Workflow Guardrails
To minimize token consumption and keep interactions fast, **YOU MUST** follow these rules:

1. **Progressive Disclosure:** Do not request full file dumps if a specific function or component shell is all you need[cite: 1]. Target your file reads to preserve context window space[cite: 1].
2. **Mandatory Planning Phase:** For edits touching more than 2 files, **do not write code immediately**[cite: 1]. First, output a brief, step-by-step bulleted plan of your strategy and ask: *"Shall we proceed with this plan?"*[cite: 1] Wait for user confirmation[cite: 1].
3. **Fail-Fast Loop:** If a fix or implementation fails twice in a row, stop guessing[cite: 1]. Acknowledge the block, step back, and propose an entirely alternative approach or ask for guidance[cite: 1].
4. **Evidence-Based Verification:** After modifying code, instruct the user to run `npm run build` or `npm run lint` to confirm success[cite: 1]. Do not report a task as complete without verifying compilation stability[cite: 1].

---

## 🛡️ Hard Constraints & Design Systems

### 🎨 Styling & Tailwind v4 Typography
- **Configuration:** Theme tokens live in the `@theme` block inside `app/globals.css`[cite: 1]. Do **not** look for or create a `tailwind.config` file[cite: 1].
- **Fonts:** All custom fonts map to CSS variables under `@theme`[cite: 1]. Never hardcode font families in JSX; always use these utilities[cite: 1]:
  - `font-brand-other` → Khand Bold (Headings)[cite: 1]
  - `font-brand-secondary-thin` → AvenirLTProRoman / Avenir 55 Roman (Body, UI text)[cite: 1]
  - `font-brand-cn` → HelveticaNowDisplay-Cn (Tags, labels)[cite: 1]
  - `font-brand-compressed` → Helvetica-Compressed[cite: 1]
- **Responsive & Layout:** The `<1023px` breakpoint is mobile; aligned with the `lg:` Tailwind prefix[cite: 1]. Mobile-specific overrides use highly specific class-name selectors or CSS `:has()` in `globals.css`—do not arbitrarily rename classes targeted there[cite: 1]. **Scrollbars (2026-07-16):** a **site-wide thin line** — global `* { scrollbar-width: thin; scrollbar-color: rgba(255,255,255,0.3) transparent }` in `globals.css` (with a `::-webkit-scrollbar` fallback for old Safari). ⚠️ Chrome/Brave 121+ **ignores all `::-webkit-scrollbar` styling on any element that sets `scrollbar-width`** — the old global `scrollbar-width: none` is exactly why custom webkit scrollbars never worked; use ONLY the standard `scrollbar-width`/`scrollbar-color` properties. Elements that must stay bar-less use inline `style={{ scrollbarWidth: "none" }}` (menu/status bars, folder panes) which beats the global rule.

### 💾 Data Layer & Environments
- **Supabase:** Single DB client (`lib/supabase.ts`) used directly in client components via public anon keys[cite: 1]. 
- **Oracle Cloud (S3-Compatible):** Used via `/api/upload` to generate presigned PUT URLs, bypassing Vercel’s 4.5 MB body limit[cite: 1].
- **Required Env Variables:** `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`[cite: 1].

---

## 🧠 Codebase Gotchas & UI Rules
*Strict boundaries to avoid breaking established layouts and wasting context cycles:*

- **Archive View Layout:** The project archive navigation uses independent containers with a blurred background overlay[cite: 1]. **Do not convert this into a standard modal UI approach.**[cite: 1]
- **Archive Display Spacing:** Maintain a wide, spread-out layout in the asset browser[cite: 1]. Keep the expanded horizontal spacing/gaps intact[cite: 1].
- **No Textures/Overlays:** Keep the dark UI clean[cite: 1]. Do not add noise textures or grain effects to the black UI hover overlays[cite: 1].
- **Asset Zoom = pan viewport (2026-07-08, supersedes the old "no zoom" rule):** **DOUBLE-clicking** an image in the open-folder window (single click is too easy to trigger while scrolling — changed 2026-07-09) zooms the image to fill **only the right assets pane** (the left description pane stays visible — changed 2026-07-13 as full-window was jarring) (cover × 1.3 headroom, computed from natural dims + viewport rect on load) with framer **drag-to-pan** (`dragConstraints` = viewport ref, `dragElastic={0}` so it hard-stops at edges). **DOUBLE-click (with drag guard) or ESC closes** — single click does nothing. Asset double-clicks are ignored for `OPEN_GUARD_MS` (500ms) after the folder opens (`openedAtRef`) — the window grows from under the cursor, so the open gesture would otherwise bleed into an accidental asset zoom. The image stays `opacity 0` until `zoomSize` is computed, then animates `scale 0.55→1` + fade (0.45s ease-out) — prevents the glitchy contain→oversize jump; do NOT render it visible pre-compute. No pinch/wheel zoom levels — one fixed zoom, pan only. No titles/metadata shown in zoom mode. The assets scroll wrapper uses the **`desc-scroll`** class (globals.css) for its styled thin scrollbar (the global thin-line scrollbar otherwise applies site-wide — see Responsive & Layout).
- **Folder Desktop (`FolderDesktop`/`FolderIcon`/`OpenFolderView` + `DesktopMenuBar`/`DesktopStatusBar` in `ArchiveClient`):** The focus view's **single full-screen OS-desktop surface** (replaced the old `PosterStack` deck + two-section scroll). A project's assets live in **folders** (`archive.folders` JSONB: `[{ id, title, description?, assets: [{ id, title, url, thumb?, size?, mime?, added?, width?, height? }] }]`). Old-vs-new comparison = **separate folders**, NOT a slider/compare widget. **Description is per-folder (2026-07-09):** the left-pane copy is `folder.description` **falling back to project-level `content`** when a folder has none (so legacy projects still show their project text). Admin has BOTH a project description box (the fallback/default) and a per-folder description textarea. **Master vs thumb (2026-07-09):** `url` = full-quality master, served ONLY in the zoom viewport; `thumb` = auto-generated ≤1600px WebP served on ALL browsing surfaces (grid covers via `firstImage` — folders-JSON-aware, list media, blur bg, preloads) through the `displayUrl(a)` helper — legacy assets without `thumb` gracefully fall back to the master. **A PDF's `thumb` is its rendered first page (2026-07-16)**, so `firstImage` treats a PDF-with-thumb as a valid image cover (skips PDFs that have none). Stored `width`/`height` now only drive **portrait detection** for the grid (the per-asset dimensions row + the old `dims` state were removed).
  - **Desktop:** straight (NO rotation), draggable icons (framer `drag`, `dragConstraints` = stage ref, positions reset every visit). **Positioned on a `ANCHORS` peripheral ring** (2026-07-13, replaced centred flex-wrap + `SCATTER` jitter): each folder is absolutely placed & CENTRED on its anchor (% of stage) which **rings the centre, avoiding the wallpaper's central safe-zone graphic** (keep-out ~x∈[32,68] y∈[28,72]) while staying in the visible mid-field (not corners) for discoverability. Anchor order = discoverability (first anchors flank the centre for low folder counts). Wrapper div does the `-translate-x/y-1/2` centre + `left/top %`; inner `motion.div` does drag via `x/y` (start 0) so the transforms don't fight. **OS click model:** **hover** shows the highlight plate `bg-white/[0.06]` with NO border; first click **selects** = plate `bg-white/[0.10]` + `border-white/20` (border only on select) + status-bar info; second click / double-click / **Enter** opens; arrows move selection (`FolderDesktop` keydown, gated by `active` prop); bare-desktop click or ESC deselects. **NO hover peek fan** — it was added then removed 2026-07-08 as gimmicky (its thumbs also poked out beside icons); do NOT re-add. Hover/arrow-selection still silently **prefetches** the folder (`prefetchFolder`). **NO hover OR drag animation at all** (scale-snap + `whileDrag` scale both removed 2026-07-08 per user — icons never scale); drag-vs-click guard via `moved` ref. Icon **dims while its window is open**; icon nodes registered via `setNode` map so Enter can measure the live (post-drag) rect.
  - **Desktop furniture:** `DesktopMenuBar` = fixed top strip (h-15, z-200): LEFT = "✕ Exit" nav (ALWAYS exits the focus view — the window's own "← Folders" title-bar nav handles folder close) + "Project Archive" wordmark + folder/asset counts; LEFT wordmark is the **`/home-focus-icon.svg` logo + "Home Focus"** (was the "Archive Focus" text until 2026-07-13). RIGHT = live clock (starts null post-mount — SSR-safe) + music toggle. **No project title / `/folder` crumb** (removed 2026-07-09). **Replaced the old floating corner buttons.** Both bars carry the `archive-header.avif` bg + `bg-black/70` overlay (added 2026-07-09; children need `relative z-10` above the overlay). `DesktopStatusBar` = fixed bottom strip (h-15, z-150, `hidden lg:flex`, `pointer-events-none`): right-aligned interaction hints from the `STATUS_HINTS` array — each = icon + text (`/right-click.png` "Click to select · Click again to open", `/arrows-icon.png` "Arrow keys to navigate", `/drag-icon.png` "Folders are draggable"), divider-separated + `/j-logo.svg` at the end; **while a folder window is open**: `justify-between` — LEFT = "Engage Live Asset" label + Instagram/LinkedIn icons (`pointer-events-auto` — the bar itself is `pointer-events-none`; props `instagramUrl`/`linkedinUrl`), RIGHT = copyright + divider + `/j-logo.svg` (logo same far-right position as closed state). The two states **crossfade** via `AnimatePresence mode="wait"` (hints ease in with fade + slight y-slide, 0.5s) so they don't snap back jitterily on folder close. Bar also carries the `archive-header.avif` bg + `bg-black/70` overlay.
  - **Mobile (`<1024px`):** tappable grid, NO drag, **single-tap opens** (select-then-open is a pointer convention).
  - **Open folder (`OpenFolderView`, keyed by folder id):** an **OS window**, not a sheet — dim `z-[140]` backdrop (desktop visible behind) + framed window (`left/right-[3.5vw] top-[84px] bottom-[76px]`, clears the h-15 menu/status bars — if bar heights change, retune these insets) that **grows out of the clicked icon** (`origin` DOMRect → initial x/y offset + scale 0.1; null origin = centre zoom; exit reverses). Window insets `top-[84px] bottom-[84px]` (equal 24px gaps above/below, clearing the h-15 bars). Mobile: full-bleed sheet below the menu bar. **Title bar** (h-14, `absolute top-0 inset-x-0 z-30` — **floats over both panes** so its `bg-black/75 backdrop-blur-md` blurs the content beneath, `rounded-t-sm`): breadcrumb path FAR LEFT = `/home-focus-icon.svg` \| Archive Focus \| {project.title} \| {folder.title}, **pipe-separated**; a plain **text "X"** (`font-brand-cn`, ~22px) FAR RIGHT closes the window (`onClose`). Panes are padded down (`pt-[88px]`/`pt-[96px]`) so content clears the floating bar but scrolls under it. The old "← Folders [ESC]" nav + folder-title/count text were removed 2026-07-09. Body panes use `order` classes — **mobile shows ASSETS first**, description below. LEFT pane (desktop `lg:w-[32%]`; **stays visible while an asset is zoomed** — the zoom viewport is confined to the right assets pane only (changed 2026-07-13; was full-window); background = a **static designed asset `/folder-description-bg.avif`** (cover/center, non-scrolling, fades in on open) under a top-lightening gradient `from-black/75 via-black/45 to-black/15`; content scrolls in a `relative z-10` inner wrapper): "Folder Description" heading (ALWAYS visible — the subtitle gate + the whole **`subtitle` field were removed 2026-07-09**; DB column left intact/unused) + body = `folder.description?.trim() || project.content` (per-folder, project fallback; `text-left`, NOT justified) + a separator `border-t` + **`folderMeta` info footer** (2026-07-09 — replaced the old metadata rows/index block): archive-header bg + `bg-black/65` overlay, inline "i" info SVG on the left, then 4 divider-separated fields each = small label over value — **Author** (`project.author`, DB column added 2026-07-09; falls back to "JUDAION (Pty) Ltd"), **Category** (`project.category`), **Upload** (`folder.assets[0].added` → `created_at`), **Type** (unique file exts joined, fallback `resource_type`); the fields row **scrolls horizontally** (`overflow-x-auto`) when they exceed the pane width) + a **closing divider** under the box (the credit-line footnote was removed 2026-07-09; pane bottom padding `pb-10 lg:pb-12` mirrors the top) + "ENGAGE LIVE ASSET" (**`lg:hidden` — mobile only**; on desktop the social links live in the bottom status bar's left side while a folder is open). **The copyright footer was removed 2026-07-09** — the copyright line now shows in the bottom status bar while a folder is open. The pane bottom is currently **empty on purpose** (a directory-path line, then a title watermark, were both trialled and removed — a comment marks the spot; user will decide a footer element later). RIGHT pane: **ambient blurred `paneThumb` bg** (`scale-110 blur-2xl` + `bg-black/70` overlay), non-scrolling `absolute inset-0` behind the inner scroll wrapper, **mounted only after the open spring settles** (`paneThumb && settled`) — a live blur over a full-res image is too costly to paint mid grow-animation. Window uses `will-change-transform`; asset imgs `decoding="async"`. Assets render in a **2-col grid** (`grid-cols-1 lg:grid-cols-2 [grid-auto-flow:dense] max-w-[1100px] mx-auto`): media + `nn` index + title underneath. **Orientation-aware (via `sizeCls` in `renderMedia`):** landscape assets `lg:col-span-2` (full width); when a folder has **≥2 portrait** assets (`gridPortraits`), each portrait takes `lg:col-span-1` so verticals sit side-by-side. A **lone/unpaired portrait** spans full width but is **height-capped (`max-h-[72vh]`) + centred** so it doesn't render huge (and a compressed thumb doesn't go soft); its caption is wrapped **with** the image in a `w-fit mx-auto` block so it hugs the image width instead of floating to the column's far left. Portrait detected via stored `width/height` or a dedicated measurement effect (`new Image()`/`<video preload=metadata>` with the handler set BEFORE `src` — the rendered element's onLoad is unreliable for preloaded/cached media). **Videos are measured & counted too** (folders' "vertical assets" are often videos, not images). **The per-asset metadata cards were REMOVED 2026-07-09**; metadata now lives in the LEFT pane's `folderMeta` block (see LEFT pane). *(A one-at-a-time framed viewer was built then reverted 2026-07-14 — the structure introduced more issues than the tilt effect was worth.)* **Per-asset descriptions and the `.ext` tag were REMOVED** (2026-07-08, focus view + admin — `folders` JSON no longer writes `description`; assets carry optional `size`/`mime`/`added` captured from the `File` on upload instead). **Click image → zoom/pan viewport** (see Asset Zoom rule above); ESC/click restores the list; empty folder = "Nothing filed here...yet".
  - **Per-folder preloading:** `loadAssetUrl` (module-level; PDFs resolve instantly) + `loadFolder` (promise-deduped: `folderLoadsRef` Map + `loadedFoldersRef` Set). `openFolderWindow` opens instantly when cached, else gates the window body behind an in-window J-logo loader (`folderReady`, 6s max-wait valve).
  - **Media rendering** inlined in `OpenFolderView.renderMedia(asset)`: images = `<img>` (double-click → zoom viewport); videos = native `<video controls>` with bg-music ducking (iOS-safe); **PDFs = first-page thumbnail (`asset.thumb`) opened via the SAME `enlarged` double-click flow as images → an inline `PdfReader` in the right pane** (legacy PDFs with no thumb fall back to a compact document card). Cursor tag shows on image + PDF hover.
  - **PDF reader (`components/PdfReader.tsx`)** renders pages to canvas via **`pdfjs-dist`** (dynamically imported; `getDocument({ url })`; worker self-hosted at `/public/pdf.worker.min.mjs`), white "paper" sheets, page nav + zoom, view-only (no download/print), canvas (works on mobile). **`variant` prop (2026-07-16):** `"inline"` (the one now in use) = `absolute inset-0` filling the folder-window **assets pane** — NOT a separate full-screen layer — with a **top-left "← Back" button** (`onClose` → clears `enlargedId`) and an **ambient spotlight**: `dominantColor()` samples page 1's main colour (skips paper-white / near-black), rendered as a centre radial glow falling off to a **black vignette**. `"overlay"` = the old full-screen `fixed z-[300]` mode (grain bg + "Exit"), kept but unused. **Zoom re-renders build the new pages offscreen and swap them in atomically** (`host.replaceChildren`) so there's no white flash / scroll jump; the first render streams into the live host under the loader. Superseded renders cancel via `renderToken`.
  - **`file_url` is kept** as the flat union of all folder asset URLs (written by the admin on every save) so masonry **grid covers** (`firstImage(item)`) + legacy reads keep working with no change. Pre-migration rows with no `folders` fall back to one derived folder (`projectFolders` memo).
  - **ESC / nav precedence:** **enlarged asset** — image zoom OR the inline PDF reader, both held in `enlargedId` (`OpenFolderView` intercepts ESC in **capture phase + `stopImmediatePropagation`**) → open folder → **desktop selection** (deselect) → close focus view. The old parent `openPdf` state + full-screen mount + `pdfOpen`/`onOpenPdf` props were **removed 2026-07-16** when the reader moved inline. Menu-bar nav is contextual ("← Folders" when a folder is open, else "✕ Exit"). Switching/closing a project collapses any open folder + selection.
  - **Deep links:** `?project=<id>&folder=<id>` — applied once after fetch (`deepLinkAppliedRef`/`pendingFolderRef`); state mirrored to the URL via `history.replaceState` (NO history entries — keeps the header's `router.back()` intact).
- **Admin folder builder (`adminportal/page.tsx`):** the form builds `folders` — add/remove/rename folders, each with a **per-folder description textarea** (2026-07-09; `setFolderDescription`, written as `folder.description`) + asset rows (file picker + **per-asset title** only; the per-asset description textarea was removed 2026-07-08). The project-level description box stays as the fallback for folders left blank. On save, pending files upload via the presigned-Oracle flow into `category/project/folder/…` paths; **`size`/`mime`/`added` are captured from the `File` at upload time** and written into the asset JSON (existing assets pass their stored metadata through; re-saving a legacy project drops any old `description` values — intentional). **Author field (2026-07-09):** admin has an optional project-level Author input; nullable `author text` column on `public.archive` (Supabase project `zxdzwnnbxnoleijeumci`); focus-view info footer reads `project.author` → falls back to "JUDAION (Pty) Ltd". (A min-description-word-count gate was trialled 2026-07-09 then removed same day.) **Auto-thumbnails (2026-07-09):** every image upload also generates a ≤1600px WebP in the browser (`makeThumb`: createImageBitmap → canvas → `toBlob("image/webp", 0.85)`) uploaded beside the master as `*_thumb.webp` → asset `thumb`; master pixel dims stored as `width`/`height`; the image thumb is skipped if it wouldn't save bytes. **PDF thumbnails (2026-07-16):** a PDF upload renders its **first page** to a WebP via `makePdfThumb` (lazy `pdfjs-dist`, same worker) and is **always** kept as `thumb` (a PDF can't otherwise be shown as an image cover); the rendered raster dims are stored as `width`/`height`. The designer NEVER makes thumbnail versions manually. ⚠️ Thumbs only generate for **newly-picked files** — existing PDF/image assets won't get one on a plain re-save unless the file is re-selected. **The project-form "Asset Type" `<select>` was removed 2026-07-16** — `resource_type` stays in the DB + Meta Manager (Category is now full-width) but new projects save it blank. Writes `folders` JSON + the flat `file_url`. Edit mode loads existing folders (legacy rows auto-wrap `file_url`).
- **Focus View Layout (`ArchiveClient`, route `/archivecatalogue`) — SINGLE full-screen section** (the two-section scroll + inertia wheel-lerp + `focusScrollRef` + scroll-down hint were all **removed**; the project description now lives in the open-folder left pane):
  - A full-screen overlay (`backdrop-blur + bg-black/88`) containing one 100%-height `<section>`: **desktop wallpaper base layer** (random-per-session from `public/archive-wallpapers/`, read by the server page `getWallpapers()` via `fs` and passed as the `wallpapers` prop → client picks one in a lazy `useState` initializer) under the grain video bg + layered gallery lighting (edge vignette + spotlight pool + floor pool). **Drop a new image into the wallpapers folder and it's auto-picked up** (build-time for the static page, per-request in dev) — no code change. `getWallpapers` filters `avif|webp|png|jpg`. OS chrome = `DesktopMenuBar` (top) + `DesktopStatusBar` (bottom). Centred selector = `FolderDesktop` (see Folder Desktop gotcha above).
  - **Asset-load gate (`focusLoading`):** on open, a J-logo/grain loader covers the view but now preloads **only the peek-fan thumbnails** (folders preload per-folder on hover-intent/open — see Per-folder preloading above), so entry is fast even for asset-heavy projects. Min-delay 1.5s (title readable, no flash) + 8s max-wait safety valve. ESC still closes during load (the loader is z-260, above the nav).

---

## 📐 Page Structure & Mobile Patterns

### `RotateNotice` — landscape phone overlay
- Renders in `ClientShell` at all times; shown/hidden purely by a CSS media query `(orientation: landscape) and (max-height: 500px)`. Targets phones held sideways only — never tablets or desktop. No JS rotation API (iOS doesn't support it). Do not add JS logic here.

### `InspectionLoader` — page-level asset gate
- Full-screen overlay. Renders the shared **`<LoaderScreen>`** (`/public/loading-screen.mp4` full-bleed, fading in on `canplay` for slow networks, + glitching J-logo pinned **bottom-right**; the old grain bg + centred "loading…" text were removed 2026-07-20). Driven by a `show` prop; `AnimatePresence` fades it out once parent assets are ready. `onExited` callback lets the parent start its assemble animation only *after* the fade completes. Used by Tier and Archive pages. **The archive-catalogue page loader and focus-view asset loader (`ArchiveClient`) use the same `<LoaderScreen>`.** `label` prop is retained on `InspectionLoader` for API compatibility but no longer rendered. NOTE: the focus-view **ambient desktop grain background** (over the wallpaper) is NOT a loader and stays grain.

### Mobile horizontal-scroll background — `ServicesClient`, `ProjectArchiveClient`
- Wide landscape bg shown full-height, scrolled horizontally on mobile.
- A **real `<img className="h-full w-auto … block">`** (NOT `background-image` + a magic `w-[300vw]`) defines the exact scroll width from the image's natural aspect → no crop / black gap / cutoff. Section is `w-max` (mobile) / `w-full` (desktop); overlays sit `absolute inset-0`. Desktop keeps CSS `background-size: cover` (+ parallax).
- ⚠️ This pattern was tried on Home and **reverted** (caused zoom/gap). Do NOT re-apply it to Home.

### Hard-stop horizontal scroll (all background pages)
- Every mobile native horizontal-scroll bg adds **`overscroll-x-none`** on the `overflow-x-auto` container to kill iOS rubber-band overscroll (which reveals black past the image edge): Home, Services, Methodology, Contact, Project Archive.

### `HomeClient` — KEEP ORIGINAL structure
- `w-[300vw]` canvas + `object-cover` bg image + framer-motion `drag="x"` content layer (drag layered over native scroll). It is intentionally NOT on the `<img>`/`w-max` pattern. Keeps its own SURGICAL-MASK fade-from-black. Mobile hero labels (Vision/Structure/Identity) hidden via `.vision/structure/identity-block-mobile { display:none }` in globals.

### Tier pages header banner — `Tier1/2/3Client`
- Banner is flush to the top via `-mt-12 lg:-mt-20` (cancels the column's `pt-12 lg:pt-20`) so `archive-header.avif` covers behind the fixed nav; inner content uses `pt-20 lg:pt-18` to drop the title below the nav. **Never use positive `mt`** here — it opens a black gap above the image.

### Services mobile tier hitboxes — `ServicesClient`
- Always-on (no hover) tappable boxes over the 3 posters → `/tier-1|2|3`. Separate `HITBOXES_MOBILE` coords (% of full image) + `MOBILE_SELECTION` config (`lineOpacity`, `handleSize`, `handleOpacity`, `scanTint`). Desktop boxes stay hover-gated.

### The Narrative (route `/thenarrative`) mobile — `TheNarrativeClient`
- Pillar + hero scanlines always-on via `opacity-100 lg:opacity-0 lg:group-hover:opacity-100`; image borders thinner on mobile (`border-[1px] lg:border-[2px]`). Section-4 body copy `hidden lg:block`; heading dropped lower via reduced `pb`.

### Audio system — `Tier1/2/3Client` + `ArchiveClient`
- Each page creates its own `new Audio()`; first user click starts + fades it in; ducks on asset-video play, restores on pause/ended.
- **iOS gotchas:** `audio.volume` is READ-ONLY → mute/duck MUST set `audio.muted` (not just volume). After a video plays, the bg `<audio>` is *paused* by iOS → restore handlers MUST call `audio.play()`. Archive video restore covers BOTH `onPause` AND `onEnded` (iOS fires only `ended` on completion).

### `IntroLoader` video
- Put `src` directly on the `<video>` (not a child `<source>`) so a React src swap reloads on iOS. Use `autoPlay muted playsInline preload="auto"`. On `isFinished`, rewind to frame 0 and play after `REVEAL_MS` (~1800ms) so the desktop clip's **epilepsy warning** (its start) isn't consumed behind the loader overlay.

### Video assets (Oracle bucket)
- All bg/intro/section videos must be web-optimised: `-movflags +faststart`, `-pix_fmt yuv420p` (iOS), audio stripped (`-an`) for muted-use clips. Never ship 4K/100MB+ masters to mobile — downscale (1080–1440p) + CRF-encode.